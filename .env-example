GITSCOPE_REPOS=
# Date-range filter chips (in days), comma-separated. Defaults to 7,30,90,365.
GITSCOPE_RANGE_PRESETS=
# Log verbosity: debug | info | warn | error. Defaults to info.
# `debug` traces every git command with its duration — useful for investigating slow loads.
GITSCOPE_LOG_LEVEL=
